local a=Class(function(self)self.cooldown=false;self.cooldowns={}end,nil,{})function a:Announce(b)if not self.cooldown and not self.cooldowns[b]then local c=TheInput:IsKeyDown(KEY_CTRL)self.cooldown=ThePlayer:DoTaskInTime(3,function()self.cooldown=false end)self.cooldowns[b]=ThePlayer:DoTaskInTime(10,function()self.cooldowns[b]=nil end)TheNet:Say(STRINGS.LMB.." "..b,c)end;return true end;return a